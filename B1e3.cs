using UnityEngine;

public class B1e3 : MonoBehaviour
{
    
    void Start()
    {
        int contador = 5;
        do
        {
            Debug.Log("jugador conectado");
            contador--;
        } while (contador > 0 );
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
